"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const micromatch_1 = __importDefault(require("micromatch"));
function isPatternMatch(string, pattern, options) {
    if (!string || !pattern) {
        return false;
    }
    // Wildcard all (*)
    if (pattern === '*') {
        return true;
    }
    // Whitelist (foo,bar)
    if (pattern.includes(',') && !pattern.includes('{')) {
        return pattern.split(',').some((part) => string === part);
    }
    // Patterns ([a-z], foo|bar, etc)
    return micromatch_1.default.isMatch(string, pattern, options);
}
exports.default = isPatternMatch;
