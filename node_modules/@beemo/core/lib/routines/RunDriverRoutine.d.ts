import DriverContext from '../contexts/DriverContext';
import { ExecuteCommandOptions } from './driver/ExecuteCommandRoutine';
import RunInWorkspacesRoutine from './RunInWorkspacesRoutine';
export default class ExecuteDriverRoutine extends RunInWorkspacesRoutine<DriverContext> {
    pipeRoutine(packageName?: string, packageRoot?: string): void;
    /**
     * When a parallel pipe "//" is defined, we need to create an additional routine
     * for each instance.
     */
    pipeParallelBuilds(key: string, options?: Partial<ExecuteCommandOptions>): void;
}
//# sourceMappingURL=RunDriverRoutine.d.ts.map