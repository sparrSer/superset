"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const execa_1 = __importDefault(require("execa"));
function getLastTag() {
    return execa_1.default('git', ['describe', '--tags', '--abbrev=0', '@^']).then(response => response.stdout.trim());
}
exports.getLastTag = getLastTag;
function getCommitsSince(since) {
    return execa_1.default('git', ['log', '--oneline', `${since}..@`]).then(response => response.stdout.trim().split('\n'));
}
exports.getCommitsSince = getCommitsSince;
let commitHash = '';
function getCommitHash() {
    if (commitHash) {
        return commitHash;
    }
    try {
        commitHash = execa_1.default.sync('git', ['rev-parse', '--short=7', 'HEAD']).stdout;
    }
    catch (error) {
        // Ignore error
    }
    return commitHash;
}
exports.getCommitHash = getCommitHash;
