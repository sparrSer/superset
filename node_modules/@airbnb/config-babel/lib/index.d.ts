import { BabelConfig } from '@beemo/driver-babel';
import { NimbusEnvSetting } from '@airbnb/nimbus-common';
interface BabelOptions {
    env?: NimbusEnvSetting;
    esm?: boolean;
    graphql?: boolean;
    library?: boolean;
    next?: boolean;
    node?: boolean;
    react?: boolean;
    typescript?: boolean;
}
/**
 * Create a root project config for a project.
 */
export declare function getConfig({ env, esm, graphql, library, next, node, react, typescript, }: BabelOptions): BabelConfig;
export {};
//# sourceMappingURL=index.d.ts.map