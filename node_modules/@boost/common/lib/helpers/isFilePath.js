"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Path_1 = __importDefault(require("../Path"));
const NIX_START = /^(\/|\.)/u;
const WIN_START = /^([A-Z]:|\.)/u;
function isFilePath(path) {
    const filePath = path instanceof Path_1.default ? path.path() : path;
    if (filePath === '') {
        return false;
    }
    // istanbul ignore next
    if (process.platform === 'win32') {
        return WIN_START.test(filePath) || filePath.includes('/') || filePath.includes('\\');
    }
    return NIX_START.test(filePath) || filePath.includes('/');
}
exports.default = isFilePath;
